# Production Database connection string (will be updated with cloud database)
DATABASE_URL="postgresql://username:password@hostname:5432/persmon_ems?schema=public"

# JWT configuration
JWT_SECRET="persmon-prod-jwt-secret-2025-secure-key-random-generated"
JWT_EXPIRES_IN="1d"

# Server configuration
PORT=3001
NODE_ENV=production
